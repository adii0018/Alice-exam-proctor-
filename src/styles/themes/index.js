import { defaultScheme } from './default';
import { oceanScheme } from './ocean';
import { forestScheme } from './forest';
import { sunsetScheme } from './sunset';

export const colorSchemes = {
  default: defaultScheme,
  ocean: oceanScheme,
  forest: forestScheme,
  sunset: sunsetScheme
};

export { defaultScheme, oceanScheme, forestScheme, sunsetScheme };
