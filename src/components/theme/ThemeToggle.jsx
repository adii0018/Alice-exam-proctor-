/**
 * ThemeToggle Component - DISABLED
 * Theme toggle has been permanently disabled.
 * App is locked to dark mode.
 */
const ThemeToggle = () => {
  // Component disabled - dark mode is permanent
  return null;
};

export default ThemeToggle;
